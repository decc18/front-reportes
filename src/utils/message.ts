const isOk = ref<boolean>(false) 
const cerrarSesion = ref<boolean>(false) 
const message = ref('')
const isMessageDialogVisible= ref<boolean>(false) 
export { cerrarSesion, isMessageDialogVisible, isOk, message }

